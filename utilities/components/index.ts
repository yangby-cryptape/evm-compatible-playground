import * as deploy from "./deploy";
import * as storage from "./storage";
import * as transfer1 from "./transfer1";
import * as transfern from "./transfern";

module.exports = {
  deploy,
  storage,
  transfer1,
  transfern,
}
